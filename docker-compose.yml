services:
  
  # database container
  # ---------------------------------------------
  database:
    build: ./database
    env_file:
      - .env/database

  # api container
  # ---------------------------------------------
  api:
    build: ./api
    ports:
      - "3000:3000"
    env_file:
      - .env/api
    depends_on:
      - "database"